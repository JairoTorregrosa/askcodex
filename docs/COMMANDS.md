# Command reference

Generated by `askcodex reference`; edit the Clap definitions to change this file.

## `askcodex`

```text
Drives the ChatGPT/Codex subscription backend (chatgpt.com/backend-api/codex) with the tokens already stored in ~/.codex/auth.json by `codex login`. No API key is used or accepted.

Usage: askcodex [OPTIONS] <COMMAND>

Commands:
  reference   Print the command reference generated from this executable's parser
  transcribe  Transcribe a WAV audio file to text
  whoami      Show account identity and plan
  usage       Show rate-limit / quota usage
  models      List available agent/LLM models and modalities
  image       Generate or edit images (always one opaque PNG; the backend picks the size)
  ask         Streaming text completion via /codex/responses
  raw         Call an arbitrary backend path (escape hatch)
  auth        Inspect or refresh the stored tokens

Options:
      --json
          One versioned JSON result (raw commands preserve the wire response)

      --events
          Versioned newline-delimited JSON events for semantic commands

      --no-refresh
          Do not auto-refresh the access token even if near expiry (also disables the 401 refresh-and-retry)

  -h, --help
          Print help (see a summary with '-h')

  -V, --version
          Print version

```

## `askcodex reference`

```text
Print the command reference generated from this executable's parser

Usage: askcodex reference [OPTIONS]

Options:
      --json
          One versioned JSON result (raw commands preserve the wire response)

      --events
          Versioned newline-delimited JSON events for semantic commands

      --no-refresh
          Do not auto-refresh the access token even if near expiry (also disables the 401 refresh-and-retry)

  -h, --help
          Print help

```

## `askcodex transcribe`

```text
Transcribe a WAV audio file to text

Usage: askcodex transcribe [OPTIONS] <FILE>

Arguments:
  <FILE>
          Regular WAV file, at most 25 MiB. Convert other formats first

Options:
      --json
          One versioned JSON result (raw commands preserve the wire response)

      --events
          Versioned newline-delimited JSON events for semantic commands

      --no-refresh
          Do not auto-refresh the access token even if near expiry (also disables the 401 refresh-and-retry)

  -h, --help
          Print help

```

## `askcodex whoami`

```text
Show account identity and plan

Usage: askcodex whoami [OPTIONS]

Options:
      --json
          One versioned JSON result (raw commands preserve the wire response)

      --events
          Versioned newline-delimited JSON events for semantic commands

      --no-refresh
          Do not auto-refresh the access token even if near expiry (also disables the 401 refresh-and-retry)

  -h, --help
          Print help

```

## `askcodex usage`

```text
Show rate-limit / quota usage

Usage: askcodex usage [OPTIONS]

Options:
      --json
          One versioned JSON result (raw commands preserve the wire response)

      --events
          Versioned newline-delimited JSON events for semantic commands

      --no-refresh
          Do not auto-refresh the access token even if near expiry (also disables the 401 refresh-and-retry)

  -h, --help
          Print help

```

## `askcodex models`

```text
List available agent/LLM models and modalities

Usage: askcodex models [OPTIONS]

Options:
      --client-version <CLIENT_VERSION>
          Client version reported to the backend (required query param)

          [default: 0.147.0]

      --json
          One versioned JSON result (raw commands preserve the wire response)

      --events
          Versioned newline-delimited JSON events for semantic commands

      --no-refresh
          Do not auto-refresh the access token even if near expiry (also disables the 401 refresh-and-retry)

  -h, --help
          Print help

```

## `askcodex image`

```text
Generate or edit images (always one opaque PNG; the backend picks the size)

Usage: askcodex image [OPTIONS] <COMMAND>

Commands:
  create  Text -> image
  edit    Edit / reference-guided image (up to 5 reference images)

Options:
      --json
          One versioned JSON result (raw commands preserve the wire response)

      --events
          Versioned newline-delimited JSON events for semantic commands

      --no-refresh
          Do not auto-refresh the access token even if near expiry (also disables the 401 refresh-and-retry)

  -h, --help
          Print help

```

## `askcodex image create`

```text
Text -> image

Usage: askcodex image create [OPTIONS] <PROMPT>

Arguments:
  <PROMPT>
          Image prompt

Options:
      --json
          One versioned JSON result (raw commands preserve the wire response)

  -o, --out <OUT>
          Output PNG path

          [default: image.png]

      --events
          Versioned newline-delimited JSON events for semantic commands

      --no-refresh
          Do not auto-refresh the access token even if near expiry (also disables the 401 refresh-and-retry)

  -h, --help
          Print help

```

## `askcodex image edit`

```text
Edit / reference-guided image (up to 5 reference images)

Usage: askcodex image edit [OPTIONS] --inputs <INPUTS>... <PROMPT>

Arguments:
  <PROMPT>
          Edit prompt

Options:
  -i, --inputs <INPUTS>...
          Regular PNG reference files, at most 25 MiB combined

      --json
          One versioned JSON result (raw commands preserve the wire response)

      --events
          Versioned newline-delimited JSON events for semantic commands

  -o, --out <OUT>
          Output PNG path

          [default: image-edited.png]

      --no-refresh
          Do not auto-refresh the access token even if near expiry (also disables the 401 refresh-and-retry)

  -h, --help
          Print help

```

## `askcodex ask`

```text
Streaming text completion via /codex/responses

Usage: askcodex ask [OPTIONS] <PROMPT>

Arguments:
  <PROMPT>
          Prompt text, or "-" to read it from stdin (UTF-8, at most 16 MiB)

Options:
      --json
          One versioned JSON result (raw commands preserve the wire response)

      --model <MODEL>
          Model slug (see `askcodex models`)

          [default: gpt-5.6-sol]

      --events
          Versioned newline-delimited JSON events for semantic commands

      --instructions <INSTRUCTIONS>
          System-style instructions

      --effort <EFFORT>
          Reasoning effort

          [default: medium]
          [possible values: low, medium, high, xhigh, max, ultra, none]

      --no-refresh
          Do not auto-refresh the access token even if near expiry (also disables the 401 refresh-and-retry)

  -h, --help
          Print help

```

## `askcodex raw`

```text
Call an arbitrary backend path (escape hatch)

Usage: askcodex raw [OPTIONS] <METHOD> <PATH>

Arguments:
  <METHOD>
          HTTP method (uppercase)

          [possible values: GET, POST, PUT, PATCH, DELETE]

  <PATH>
          Backend path (e.g. /codex/usage) or a full https URL on the backend origin

Options:
      --body <BODY>
          JSON request body as a string, or "-" to read it from stdin (UTF-8, at most 16 MiB)

      --json
          One versioned JSON result (raw commands preserve the wire response)

      --events
          Versioned newline-delimited JSON events for semantic commands

      --stream
          Stream the raw SSE response to stdout

      --no-refresh
          Do not auto-refresh the access token even if near expiry (also disables the 401 refresh-and-retry)

  -h, --help
          Print help

```

## `askcodex auth`

```text
Inspect or refresh the stored tokens

Usage: askcodex auth [OPTIONS] <COMMAND>

Commands:
  status   Show token status (never prints secret values)
  refresh  Force a token refresh now (rotates and persists tokens)

Options:
      --json
          One versioned JSON result (raw commands preserve the wire response)

      --events
          Versioned newline-delimited JSON events for semantic commands

      --no-refresh
          Do not auto-refresh the access token even if near expiry (also disables the 401 refresh-and-retry)

  -h, --help
          Print help

```

## `askcodex auth status`

```text
Show token status (never prints secret values)

Usage: askcodex auth status [OPTIONS]

Options:
      --json
          One versioned JSON result (raw commands preserve the wire response)

      --events
          Versioned newline-delimited JSON events for semantic commands

      --no-refresh
          Do not auto-refresh the access token even if near expiry (also disables the 401 refresh-and-retry)

  -h, --help
          Print help

```

## `askcodex auth refresh`

```text
Force a token refresh now (rotates and persists tokens)

Usage: askcodex auth refresh [OPTIONS]

Options:
      --json
          One versioned JSON result (raw commands preserve the wire response)

      --events
          Versioned newline-delimited JSON events for semantic commands

      --no-refresh
          Do not auto-refresh the access token even if near expiry (also disables the 401 refresh-and-retry)

  -h, --help
          Print help

```
